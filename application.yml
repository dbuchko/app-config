security:
  basic:
    enabled: false

management:
  security:
    enabled: false

logging:
  level:
    io:
      pivotal: DEBUG

---
spring:
  profiles: cloud
eureka:
  instance:
    hostname: service-registry-db.cfapps.io
    nonSecurePort: 80
